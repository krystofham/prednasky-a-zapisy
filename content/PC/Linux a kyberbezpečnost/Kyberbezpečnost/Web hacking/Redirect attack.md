`url=`, `redirect=`, `next=`, `return=`, `returnUrl=` apod.

Bug:
Web aplikace vezme URL od uživatele a **bez ověření ho na ni přesměruje**

**Phishing**
- URL začíná důvěryhodnou doménou
- uživatel očekává legitimní stránku
- aplikace ho přesměruje jinam